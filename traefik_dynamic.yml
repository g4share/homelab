http:
  routers:
    forward:
      rule: Host(`jellyfin.${domain_name}.com`)
      service: forward

  services:
    forward:
      loadBalancer:
        servers:
          - url: http://10.0.0.10:8096